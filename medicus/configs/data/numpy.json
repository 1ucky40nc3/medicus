{
    "dataset": {
        "name": "SharedTransformNumpyDataset",
        "config": {
            "samples_dir": "{dataset_dir}/{dataset_name}/train/samples",
            "targets_dir": "{dataset_dir}/{dataset_name}/train/targets"
        },
        "transforms": [
            ["Lambda", ["from_numpy"]],
            ["Resize", [[104, 104]]]
        ]
    },
    "dataloader": {
        "name": "DataLoader",
        "config": {
            "batch_size": null
        }
    }
}